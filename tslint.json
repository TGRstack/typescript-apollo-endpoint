{
  "extends": [
    "@tgrx/tslint-config-tgr"
  ],
  "rules": {
    "no-implicit-dependencies": [
      // config
      true, "dev",
      // src subdirectories
      ["config", "gql", "modules", "types", "utils"]
    ],
    "no-submodule-imports": [
      // config
      true,
      // src subdirectories
      "config", "gql", "modules", "types", "utils",
      // imports
      "colors"
    ]
  }
}